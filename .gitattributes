# Normalize all text to LF in the repo (so Linux CI + bash scripts always see LF).
* text=auto eol=lf
# Windows batch launcher keeps CRLF.
*.bat text eol=crlf
# The merged PGO profile is binary.
*.profdata binary
